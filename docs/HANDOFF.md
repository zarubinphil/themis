# Хэндофф — ремонт конвейера Фемиды через Гелиоз

**Состояние:** ремонт R1-R7 закрыт. R7 исправлен внешним коммитом в Мнемозине.

## Что сделано
- Дом миссии Гелиоза стоит в Фемиде: `config/`, `queue/tasks/R1-R7`, `.helioz/state/`.
- Разбор и ponytail-пас: `docs/РЕМОНТ-РАЗБОР.md` (31 ход описан, 15 файлов правок сжаты до 8).
- Приёмка исполняема: `bash scripts/priemka_remont.sh` — десять команд Части 8. Было 1 из 10 зелёных, стало **7 из 10**.
- **R1 done** (маркер): гейты `verdict.py` (словарь, md5, лимит раундов), зубы `themis_status.py`, лок `.agent/drafts/` в `claude_guard.py`.
- **R4 done** (маркер): `document_guard.py --md-only` и `--rules`, починка денежного правила, указатель в `DOCX_FORMATTING.md`.
- **R2-R6 done** (маркеры): тонкий проводник, один черновик, md-only guard, рецензия тремя линзами, корпус ГК/АПК и диагностика приборов.
- **R7 done**: `mnemazine` commit `83e74bb` — тест читает файл с телом конвейера, не shim.

## Что дальше
1. Контрольный прогон на копии дела из прогона 25.08 (каталог копии — в `$THEMIS_CASE_PROBE`, `.agent` вычищен, оригинал цел).
2. Разобрать чужой staged-хвост в рабочем дереве отдельно: `.venv/`, `knowledge/kadry/`, `scripts/tender_*`.

## Ключевое состояние
- Потолок $120, факт **$153,25 / 41 397 886 токенов** за 8 прогонов CLI. Окно с 25.08 17:47 UTC.
- Живой CLI один: `claude`. codex — лимит квоты до 23.09, kimi — 500 от провайдера. Проверяющий поэтому отдельный процесс того же CLI, слепой к отчёту исполнителя.
- Запись под `.claude/` режет встроенный гейт чувствительных путей: исполнитель кладёт файл в `.helioz/state/inbox/`, перенос делает оркестратор.
- Исполнителям выдан Bash через `--allowedTools` (`config/clis.json`); `--dangerously-skip-permissions` не применялся.

## Ссылки
- Задание владельца: `локальный файл ремонта ФЕМИДЫ`
- Коммиты: `de9bf12` база · `27115e9` R1 · `f2dd790` + `121927b` R4 · `a6fa5a0` R6 · `83e74bb` R7 в Мнемозине
- Логи прогонов: `.helioz/state/logs/`, маркеры: `.helioz/state/markers/`
