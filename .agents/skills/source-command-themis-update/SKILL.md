---
name: "source-command-themis-update"
description: "Обновить Themis с GitHub — только логика (агенты, скиллы, скрипты, cockpit), данные дел не трогаются; git-статус до/после, откат при сбое"
---

# source-command-themis-update

Use this skill when the user asks to run the migrated source command `themis-update`.

## Command Template

Обнови Themis строго по скиллу `themis-update` (полный алгоритм, откат при сбое, сверка целости данных, контракт доклада — там). Поверх скилла не импровизировать.
