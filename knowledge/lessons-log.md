# Журнал уроков (самообучение)

> Сюда Фемида заносит системные уроки из ошибок прогонов (причина → исправление → как не повторять).
> Правки доверителя по документам — в `redlines.md`.

## 14.07.2026 — правки доверителя в `.docx` молча затёрты пересборкой из скрипта, выжили только в `_baselines/`

Доверитель по ходу сессии открывал уже выданные `.docx` (`prikaz-2-sluzhebnaya-proverka_v1`, `uvedomlenie-bilalov_v1`, `uvedomlenie-bank-alfa_v1`, `instrukcia-dlya-bilalovoy_v1`) и правил их напрямую в Word/аналоге — не через чат. Пока файл был открыт у него, `create_docx.py`'s `DocBuilder.save()` пытался перезаписать `_baselines/<имя>.docx` через `shutil.copy2` и ловил `OSError` (файл занят другим приложением) — исключение проглочено (`except OSError: pass`), без предупреждения в лог/чат. В результате: рабочий файл в `03_drafts/` перезаписался моим свежим прогоном (правки доверителя исчезли из него), а его правки уцелели только в `_baselines/` — то есть папка «снимок ДО» внезапно стала единственным местом, где лежит «ПОСЛЕ». Обнаружено случайно, только благодаря `cp:lastModifiedBy`/`cp:revision` в `docProps/core.xml` каждого `.docx` (у нетронутых файлов пусто и revision=1, у правленых — «Filipp doveritel» и revision 2-3): без этой проверки я бы принял направление diff'а наоборот и приписал доверителю правки, которые на самом деле были моими собственными более ранними черновиками.

- **Причина:** пересборка документа из Python-скрипта поверх уже выданного и потенциально открытого доверителем файла — без проверки, не изменился ли файл с момента выдачи, и без видимого сигнала при сбое копирования baseline.
- **Исправлено (в этой сессии):** проверил `docProps/core.xml` (`lastModifiedBy`/`revision`) каждого затронутого `.docx`, чтобы понять реальное направление правки прежде чем читать diff; перенёс найденные правки в `redlines.md`, руками применил их к мастер-версиям.
- **Системное правило:** перед тем как пересобирать/перезаписывать уже выданный `.docx` из скрипта (а не через `Edit`), сначала сравнить его текущее содержимое с последним известным моим выводом (или с `_baselines/`, если он не тронут). Если отличается — считать это правкой доверителя, не перезаписывать вслепую, сначала спросить или слить правки. И: `except OSError: pass` в `create_docx.py.save()` — тихий отказ снимка недопустим, минимум печатать предупреждение в stdout при сбое копирования baseline, иначе несовпадение обнаруживается только случайно (как здесь — через ручную проверку XML-метаданных).

## 10.07.2026 — «протокол Фемида» вызван из чужой сессии, дело собрано вручную мимо конвейера

Клиент сказал «протокол Фемида. Новое дело» в сессии Claude Code, открытой в `~/Проекты/CRM`, не в `~/Проекты/themis`. Агент этой чужой сессии не знал о существовании проекта (не было ссылки ни в его памяти, ни в контексте), собрал материалы и составил претензию+обращение финуполномоченному вручную (генерик python-docx, WebSearch вместо `practice_index.md`/охотников, облачный vision вместо Apple Vision), сохранил вне `cases/`. Пойман пользователем на несоответствии месту сохранения, дело перенесено в `cases/doveritel-7/dtp-osago-2026/` задним числом (см. `cases/_logs/session_10-07-2026.md`).

- **Причина:** триггер-фраза «протокол Фемида» нигде не задокументирована за пределами этого проекта.
- **Исправлено:** добавлена строка про `~/Проекты/themis` в `~/.claude/references/owner.md` (глобальный, читается всеми сессиями) с явным указанием — трещотка «протокол Фемида»/юридическое дело в чужой сессии обязана переадресовать на новую сессию здесь, а не исполнять вручную.
- **Системное правило:** если в `00_intake/` дела оказываются файлы, уже частично обработанные снаружи (есть готовый `.docx`-черновик без сопровождающих `knowledge-map.md`/`practice.md` от штатного конвейера) — не доверять содержимому черновика вслепую, прогнать материал через `case-mapper` и как минимум `doc-reviewer` (Кони) заново, даже если черновик выглядит завершённым.

## 19.07.2026 — инбокс содержал файлы нескольких дел; новое дело отфильтровано по метке времени

В `~/Desktop/inbox/` скопилось ~100 файлов разных дел (Доверитель/Доверитель, кадровые договоры «Ваш партнер», ДДУ и пр.). Прежний прогон протокола смешал их, пользователь остановил работу: «протокол сработал неправильно и не принес необходимые документы — изучай только добавленные сегодня в последний час». Дело `doveritel-5/dtp-2026` собрано из 3 сканов с меткой времени 18:21–18:27 текущего дня; остальные файлы оставлены в инбоксе нетронутыми.

- **Причина:** команда `/new-case` по умолчанию перемещает ВСЕ файлы инбокса в одно новое дело, что корректно только при монотематическом инбоксе.
- **Исправлено:** фильтр по метке времени поступления (последний час) + тематическая сверка содержимого (OCR первой страницы) до перемещения.
- **Системное правило:** перед массовым перемещением инбокса в `00_intake/` — группировать файлы по времени поступления и тематике; перемещать только группу текущего дела, остальное оставлять на месте с записью в `_index.md` → раздел Inbox. При сомнении — спросить, а не перемещать все подряд.
