{
  "_": "Образец конфигурации автономного цикла. Копировать, править, запускать:",
  "__": "  python3 scripts/autoloop.py моя-задача.json          — боевой прогон",
  "___": "  python3 scripts/autoloop.py моя-задача.json --dry    — проверить конфигурацию и гейт",
  "____": "  python3 scripts/autoloop.py --replay                — пересобрать утренний отчёт",
  "_____": "Поля с подчёркиванием игнорируются: JSON не знает комментариев.",

  "task": "Этап 5 — написать pii_gate.py: fail-closed обезличивание с обратимой картой",
  "_task": "Цель одной строкой: исход + граница. Без неё у цикла нет условия остановки.",

  "stage": "5",
  "_stage": "Автономно исполняются только 1, 2 и 5. Этап 3 (миграция) — никогда: 77 дел необратимо.",

  "guards": {
    "max_iterations": 12,
    "max_money": 15.0,
    "wall_clock_seconds": 28800,
    "no_progress_limit": 2,
    "stop_when": "gate_green"
  },
  "_guards": "Все четыре обязательны, иначе цикл не стартует. max_money считается прибором token_ledger с диска, а не самоотчётом модели. no_progress_limit — сколько итераций подряд вердикт гейта может не сдвигаться, прежде чем это признают застреванием.",

  "isolation_worktree": true,
  "_isolation": "Параллельные роли получают по своей рабочей копии git worktree. Выключить можно только когда параллельная роль одна.",

  "roles": [
    {
      "name": "stroitel",
      "kind": "generator",
      "parallel": true,
      "argv": ["claude", "-p", "--model", "sonnet", "{brief}"]
    },
    {
      "name": "skeptik",
      "kind": "reviewer",
      "parallel": true,
      "argv": ["claude", "-p", "--model", "opus", "{brief}"]
    }
  ],
  "_roles": "Нужен хотя бы один автор и хотя бы один рецензент. {brief} подставляется заданием: цель, номер итерации, вердикт гейта — и НИКОГДА вывод соседней роли. Поле verdict_source запрещено: пасс держит код возврата гейта, мнение модели вердиктом не считается. argv только массивом строк — строка-команда открывает инъекцию через аргумент. Установка пакетов в argv отбивается до старта.",

  "gate": ["{python}", "scripts/loop_gate.py", "--json"],
  "_gate": "Единственный источник вердикта. Компиляция, --selftest затронутых приборов, smoke на ivanov-ivan, ПД-сторож. Неразобранный вердикт считается КРАСНЫМ."
}
